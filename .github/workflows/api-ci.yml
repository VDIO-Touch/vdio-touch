name: Build and publish API server docker image
on:
  release:
    types: [published]
jobs:
  setup-build-publish:
    name: Setup, Build, and Publish Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Validate CalVer tag
        run: |
          echo "${{ github.event.release.tag_name }}" | grep -Eq "^[0-9]{4}\.[0-9]{2}\.[0-9]+$" || (echo "❌ Invalid CalVer tag. Expected format: YYYY.MM.PATCH (e.g. 2026.03.1)" && exit 1)

      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: server build and push to registry
        uses: docker/build-push-action@v5
        with:
          context: ./server/api
          file: ./server/api/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/video-touch-api:latest
            ${{ secrets.DOCKER_USERNAME }}/video-touch-api:${{ github.event.release.tag_name }}
